import 'package:flutter/material.dart';

class ArticleList extends StatelessWidget {
  ArticleList({@required data});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
